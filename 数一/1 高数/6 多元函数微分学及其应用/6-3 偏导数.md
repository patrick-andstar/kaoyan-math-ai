---
type: 基础知识
title: "[[6-3 偏导数]]"
tags:
  - 高数
  - 多元函数微分学及其应用
aliases:
  - 偏导数
---
### 定义
设函数 $z = f (x, y)$ 在点 $(x_0, y_0)$ 的某邻域内有定义，若极限：  
$$  
\lim_{\Delta x \to 0} \frac{f(x_0 + \Delta x, y_0) - f(x_0, y_0)}{\Delta x}  
$$ 
存在，则称此极限为 $z = f (x, y)$ 在点 $(x_0, y_0)$ 处 **对 $x$ 的偏导数**，记作：  
$$  
\left. \frac{\partial z}{\partial x} \right|_{\substack{x=x_0 \\ y=y_0}}, \quad \left. \frac{\partial f}{\partial x} \right|_{\substack{x=x_0 \\ y=y_0}}, \quad z'_x \bigg|_{\substack{x=x_0 \\ y=y_0}}, \quad \text{或 } f'_x(x_0, y_0)  
$$

类似地，函数 $z = f (x, y)$ 在点 $(x_0, y_0)$ 处 **对 $y$ 的偏导数** 定义为：  
$$  
f'_y(x_0, y_0) = \lim_{\Delta y \to 0} \frac{f(x_0, y_0 + \Delta y) - f(x_0, y_0)}{\Delta y}  
$$
### 区域上的偏导数  
若 $z = f (x, y)$ 在 **区域 $D$** 内每一点 $(x, y)$ 处都存在偏导数，则这些偏导数仍是 $x, y$ 的函数，称为 **偏导函数**（简称“偏导数”），记作：  
$$  
\frac{\partial z}{\partial x}, \quad \frac{\partial f}{\partial x}, \quad z'_x, \quad \text{或 } f'_x(x, y)  
$$
$$  
\frac{\partial z}{\partial y}, \quad \frac{\partial f}{\partial y}, \quad z'_y, \quad \text{或 } f'_y(x, y)  
$$
### 几何意义  
设有二元函数 $z = f (x, y)$，且 $z_0 = f (x_0, y_0)$：  
- $f'_x (x_0, y_0)$ 几何意义：曲线 $\begin{cases} z = f (x, y) \\ y = y_0 \end{cases}$ 在点 $(x_0, y_0, z_0)$ 处的切线对 **x 轴** 的斜率；  
- $f'_y (x_0, y_0)$ 几何意义：曲线 $\begin{cases} z = f (x, y) \\ x = x_0 \end{cases}$ 在点 $(x_0, y_0, z_0)$ 处的切线对 **y 轴** 的斜率。  

### 高阶偏导数  
#### 二阶偏导数的定义（以 $z = f (x, y)$ 为例）  
$$  
\frac{\partial^2 z}{\partial x^2} = \frac{\partial}{\partial x}\left( \frac{\partial z}{\partial x} \right) = f''_{xx}(x, y) = z''_{xx}  
$$  $$  
\frac{\partial^2 z}{\partial y^2} = \frac{\partial}{\partial y}\left( \frac{\partial z}{\partial y} \right) = f''_{yy}(x, y) = z''_{yy}  
$$
$$  
\frac{\partial^2 z}{\partial x \partial y} = \frac{\partial}{\partial y}\left( \frac{\partial z}{\partial x} \right) = f''_{xy}(x, y) = z''_{xy}  
$$  
$$  
\frac{\partial^2 z}{\partial y \partial x} = \frac{\partial}{\partial x}\left( \frac{\partial z}{\partial y} \right) = f''_{yx}(x, y) = z''_{yx}  
$$ 
类似可定义 $n \geqslant 3$ 阶偏导数。  

### 混合偏导数的连续性与次序无关性  
二阶混合偏导数在连续条件下，与求导次序无关：
若函数 $z = f (x, y)$ 的两个二阶混合偏导数 $\dfrac{\partial^2 z}{\partial x \partial y}$ 和 $\dfrac{\partial^2 z}{\partial y \partial x}$ 在区域 $D$ 内 **连续**，则在区域 $D$ 内：$\frac{\partial^2 z}{\partial x \partial y} = \frac{\partial^2 z}{\partial y \partial x}$，。 ^4c6sq1