---
type: 基础知识
title: "[[6-4 可微]]"
tags:
  - 高数
  - 多元函数微分学及其应用
aliases:
  - 全微分
  - 可微
---
### 定义  
设函数 $z=f(x,y)$ 在点 $(x,y)$ 的某邻域内有定义, 如果函数在点的`全增量` $\Delta z=f(x+\Delta x,y+\Delta y)-f(x,y)$ 可表示为$$  \Delta z = A\Delta x + B\Delta y + o(\rho)  $$其中：  
- $A, B$ 仅与点 $(x, y)$ 有关（与 $\Delta x, \Delta y$ 无关）；  
- $\rho = \sqrt{(\Delta x)^2 + (\Delta y)^2}$，且当 $\Delta x \to 0, \Delta y \to 0$ 时，$o (\rho)$ 是 $\rho$ 的高阶无穷小。  
则称 $z = f (x, y)$ 在点 $(x, y)$ 处 **可微分**，$A\Delta x + B\Delta y$ 为函数在该点的`全微分`，记为：  $$  \mathrm{d}z = A\Delta x + B\Delta y = A\mathrm{d}x + B\mathrm{d}y  $$ 
### 可微的必要条件  
若 $z = f (x, y)$ 在点 $(x, y)$ 处`可微`，则该点的**偏导数必存在**；$A = \frac{\partial z}{\partial x}, B = \frac{\partial z}{\partial y}$ ,且函数在点的全微分为$\mathrm{d}z = \frac{\partial z}{\partial x}\mathrm{d}x + \frac{\partial z}{\partial y}\mathrm{d}y$

### 可微的充分条件  
若函数 $z = f (x, y)$ 在点 $(x, y)$ 处的 **偏导数存在且连续**，则该函数在该点处**可微**。

> $z = f (x, y)$ 在点 $(0, 0)$ 处的可微的一个充分条件是 $$  \lim_{(x,y) \to (0,0)} \frac{f(x,y) - f(0,0)}{\sqrt{x^2 + y^2}} = 0  $$可参考高数强化讲义中的“非常经典十字架函数”题型
> 可参考高数强化讲义中的“反例法”题型

### 可微等价形式
> [!tip] 可微等价形式
> - **全增量形式**：  $$  \Delta z = f(x_0 + \Delta x, y_0 + \Delta y) - f(x_0, y_0) = A\Delta x + B\Delta y + o(\rho)  $$  （其中 $\rho = \sqrt{(\Delta x)^2 + (\Delta y)^2}$）  
> -  **极限形式（一）**：  $$  \lim_{\substack{\Delta x \to 0 \\ \Delta y \to 0}} \frac{\left[ f(x_0 + \Delta x, y_0 + \Delta y) - f(x_0, y_0) \right] - (A\Delta x + B\Delta y)}{\sqrt{(\Delta x)^2 + (\Delta y)^2}} = 0  $$  
> -  **局部线性形式**：  $$\Delta z = f(x, y) - f(x_0, y_0) = A(x - x_0) + B(y - y_0) + o(\rho)  $$  （其中 $\rho = \sqrt{(x - x_0)^2 + (y - y_0)^2}$）  
> -  **极限形式（二）**：  $$  \lim_{\substack{x \to x_0 \\ y \to y_0}} \frac{\left[ f(x, y) - f(x_0, y_0) \right] - \left[ A(x - x_0) + B(y - y_0) \right]}{\sqrt{(x - x_0)^2 + (y - y_0)^2}} = 0  $$
> 以上四式是 $f (x, y)$ 在点 $(x_0, y_0)$ 处 **可微的等价形式**，由任意一式均可推出：  
> - $f (x, y)$ 在 $(x_0, y_0)$ 处可微；  
> - 偏导数满足 $f'_x (x_0, y_0) = A$，$f'_y (x_0, y_0) = B$。
 ^1

可联想全微分方程