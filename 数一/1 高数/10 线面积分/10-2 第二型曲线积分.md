---
type: 基础知识
title: "[[10-2 第二型曲线积分]]"
tags:
  - 高数
  - 线面积分
aliases:
  - 第二型曲线积分
---

**对坐标的曲线积分**，可正可负
二维或三维空间中，沿给定曲线对向量场进行积分，计算向量场与曲线切线方向的点乘在曲线上的积分。
$$ \int_C \mathbf{F} \cdot d\mathbf{r} $$
向量场的曲线积分主要解决变力沿曲线做功问题, 这里的$F$就可以理解为向量场(重力场 电场 磁场等), 即为每个位置矢量$r$映射一个向量$F$, $F$沿着特定的方向$r$的分量即为在这个方向上所作的功

## 计算方法(平面)
1. [[先代后求]]
2. 分量形式在二维情况下，向量场 $\mathbf{F}$ 和曲线微分 $\mathbf{r}$ 可分解为各坐标分量：
$$
\int_C \mathbf{F} \cdot d\mathbf{r} = \int_C P(x, y) dx + \int_C Q(x, y) dy
$$曲线积分可转化为对坐标的积分。

3. 参数化形式，若曲线 $C$ 的参数方程为：
$$
\left\{
\begin{aligned}
x &= \phi(t) \\
y &= \psi(t)
\end{aligned}
\right., \quad \alpha \leq t \leq \beta
$$则积分可以表示为参数 $t$ 的定积分：
$$
\int_C P(x, y) dx + Q(x, y) dy = \int_\alpha^\beta \left[ P(\phi(t), \psi(t)) \phi'(t) + Q(\phi(t), \psi(t)) \psi'(t) \right] dt
$$

3. 如果 $x$ 和 $y$ 存在显式关系 $y = \phi(x)$，积分可以简化为：
$$\int_C \mathbf{F} \cdot d\mathbf{r} = \int_{x_1}^{x_2} P(x, \phi(x)) + Q(x, \phi(x)) \phi'(x) dx$$
4. 格林公式 ![[格林公式]]
---
## 两类线积分联系
[[10-1 第一型曲线积分|第一型曲线积分]]
平面曲线弧 $L$ 上的两类曲线积分之间有如下联系:
$$
\int_L Pdx + Qdy = \int_L (P \cos\alpha + Q \cos\beta)ds,
$$
其中 $\alpha(x, y), \beta(x, y)$ 为有向曲线弧 $L$ 在点 $(x, y)$ 处的切向量的方向角.

类似地, **空间曲线弧** $\Gamma$ 上的两类曲线积分之间有如下联系:
$$
\int_\Gamma Pdx + Qdy + Rdz = \int_\Gamma (P \cos\alpha + Q \cos\beta + R \cos\gamma)ds,
$$
其中 $\alpha(x, y, z), \beta(x, y, z), \gamma(x, y, z)$ 为有向曲线弧 $\Gamma$ 在点 $(x, y, z)$ 处的切向量的方向角.

---
## 计算方法 (空间)
### (1) 参数方程
设分段光滑的曲线 L 由参数方程 $x=x(t), y=y(t), z=z(t), t \in [\alpha, \beta]$ 给出, 其起点和终点分别对应参数 $t=\alpha$ 和 $t=\beta$, P, Q, R 在 L 上连续, 则
$$
\int_L P(x,y,z)dx + Q(x,y,z)dy + R(x,y,z)dz \\
= \int_\alpha^\beta \{P[x(t),y(t),z(t)]x'(t) + Q[x(t),y(t),z(t)]y'(t) + R[x(t),y(t),z(t)]z'(t)\} dt.
$$

### (2) 斯托克斯公式
![[斯托克斯公式]]

---
## 应用
变力做功


