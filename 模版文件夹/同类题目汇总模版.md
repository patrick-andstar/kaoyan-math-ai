---
<%*
const rawTag = await tp.system.prompt("请输入汇总标签（不带 #）");
const summaryTag = rawTag && rawTag.trim() ? rawTag.trim() : "待补充";
-%>
type: 同类题目汇总
title: "[[<% tp.file.title %>]]"
tags:
  - <% summaryTag %>
rating:
aliases:
  - <% tp.file.title %>
date_creation: <% tp.file.creation_date("YYYY-MM-DD") %>
---

## 方法汇总

```dataview
table method as "方法", rating as "评分"
from #<% summaryTag %>
where type="题目" or type="方法"
sort rating desc,file.name asc
```

## 题目汇总

```dataview
table embed(link(cover)) as "题目", method as "方法", status as "掌握状态", rating as "评分"
from #<% summaryTag %>
where type="题目"
sort rating desc,file.name asc
```
