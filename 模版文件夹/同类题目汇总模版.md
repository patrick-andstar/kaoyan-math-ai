---
<%*
const rawTag = await tp.system.prompt("请输入汇总标签（不带 #）");
const summaryTag = rawTag && rawTag.trim() ? rawTag.trim() : "待补充";
const subject = await tp.system.suggester(["高数", "线代", "概率论", "待分类"], ["高数", "线代", "概率论", "待分类"], false, "选择学科");
-%>
type: 同类题目汇总
title: "[[<% tp.file.title %>]]"
tags:
  - <% subject %>
  - <% summaryTag %>
aliases:
  - <% tp.file.title %>
source:
status: ai-draft
rating:
date_creation: <% tp.file.creation_date("YYYY-MM-DD") %>
---

# <% tp.file.title %>

> [!note] 汇总口径
> 当前页面汇总标签：`#<% summaryTag %>`。题目、错题、方法卡只要带这个标签，就会进入下方 Dataview。

## 方法汇总

```dataview
table rating as "评分", source as "来源"
from #<% summaryTag %>
where type = "方法"
sort rating desc, file.name asc
```

## 题目汇总

```dataview
table method as "方法", status as "掌握状态", rating as "评分", source as "来源"
from #<% summaryTag %>
where type = "题目" or type = "错题"
sort rating desc, status asc, file.name asc
```

## 题图速览

```dataview
table embed(link(cover)) as "题图", method as "方法", status as "掌握状态", rating as "评分"
from #<% summaryTag %>
where (type = "题目" or type = "错题") and cover
sort rating desc, file.name asc
```

## 复习策略

> [!tip] 训练顺序
> 1. 先看方法汇总，确认入口和边界。
> 2. 再遮住答案重做高评分错题。
> 3. 最后补充新的反例或变形。

## 待补充

- [ ] 同类题的共同识别信号：
- [ ] 最容易误用的方法：
