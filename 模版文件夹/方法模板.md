---
<%*
const subject = await tp.system.suggester(["高数", "线代", "概率论", "待分类"], ["高数", "线代", "概率论", "待分类"], false, "选择学科");
const chapter = await tp.system.prompt("章节标签（不带 #）") || "待分章";
const rating = await tp.system.suggester(["5", "4", "3", "2", "1", "待评估"], ["5", "4", "3", "2", "1", "待评估"], false, "方法优先级");
const source = await tp.system.prompt("来源，可留空") || "";
-%>
type: 方法
title: "[[<% tp.file.title %>]]"
tags:
  - <% subject %>
  - <% chapter %>
aliases:
  - <% tp.file.title %>
source: "<% source %>"
status: ai-draft
rating: <% rating %>
date_creation: <% tp.file.creation_date("YYYY-MM-DD") %>
weight:
---

# <% tp.file.title %>

> [!note] 方法定位
> 这张卡只写可复用套路：遇到什么信号、为什么能用、怎么操作、哪里不能套。

## 适用场景

- 题目特征：
- 常见问法：
- 所属章节：[[相关章节思维导图]]
- 关联基础：[[相关基础知识]]
- 权重：基础 / 高频 / 易混 / 后续依赖

## 识别信号

> [!question] 看到这些信号优先想到它
> - 信号 1：
> - 信号 2：
> - 信号 3：

| 命题信号 | 方法入口 | 易错提醒 |
| --- | --- | --- |
|  | 本方法 |  |

## 操作步骤

1. 确认前提条件：
2. 选择变形 / 代换 / 构造：
3. 执行计算：
4. 回代或检验：
5. 写出结论：

## 适用边界

> [!warning] 不能套用的情况
> - 条件缺失：
> - 极端情况：
> - 容易混淆的方法：

## 反例 / 边界情况

| 结论边界 | 反例或提醒 | 修正动作 |
| --- | --- | --- |
|  |  |  |

## 典型题目

```dataview
table status as "掌握状态", rating as "评分", source as "来源"
from #<% chapter %>
where type = "题目" or type = "错题"
where contains(method, this.file.name) or contains(file.outlinks, this.file.link)
sort rating desc, file.name asc
```

## 举一反三

- 可以迁移到：
- 与 [[相近方法]] 的区别：
- 需要补的基础：

## 错题回流

| 错题链接 | 错因标签 | 回流动作 |
| --- | --- | --- |
|  |  | 回流到基础知识 / 操作步骤 / 适用边界 / 反例 |

## 最小记忆清单

- 待补充：

## 待确认

- [ ] 适用条件是否完整：
- [ ] 反例是否足够：
- [ ] 典型题目是否已核对题干、选项和答案：
