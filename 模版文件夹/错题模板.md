---
<%*
let title = tp.file.title 
// 获取当前文件路径并提取父文件夹作为前缀
let path = tp.file.path();
// 分割路径并过滤空值（处理不同系统路径格式兼容）
let pathParts = path.split(/[\\/]/).filter(part => part.trim() !== '');
// 提取父文件夹名称（路径中倒数第二个部分为目标文件夹）
let parentFolder = pathParts.length >= 2 ? pathParts[pathParts.length - 2] : '';
// 生成前缀（文件夹名+短横线，无文件夹则为空）
let prefix = parentFolder ? `${parentFolder}-` : '';

if (title.startsWith("未命名")){
	title = await tp.system.prompt("请输入标题");
	if(!title) return;
	// 自动添加前缀
	title = prefix + title;
}
if(title ==""){
	title = "未命名";
} else{
	await tp.file.rename(title);
}
-%>
type: 题目
title: "[[<% tp.file.title %>]]"
status: <% tp.system.suggester(["完全掌握","比较掌握","完全不会"],["完全掌握","比较掌握","完全不会"],true,"掌握状态") %>
tags: 
method:
rating: 
source:
date_creation: <% tp.file.creation_date("YYYY-MM-DD") %>
cover: "[[<% title %>.png]]"
---
## 题目


## 复盘

- 错因：
- 关键一步：
- 正确入口：


## 举一反三

- 同类信号：
- 可迁移方法：
- 关联笔记：
