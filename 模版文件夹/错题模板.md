---
<%*
let title = tp.file.title;
const path = tp.file.path();
const pathParts = path.split(/[\\/]/).filter(part => part.trim() !== "");
const parentFolder = pathParts.length >= 2 ? pathParts[pathParts.length - 2] : "";
const prefix = parentFolder ? `${parentFolder}-` : "";

if (title.startsWith("未命名") || title.startsWith("Untitled")) {
  title = await tp.system.prompt("请输入题目标题");
  if (!title) return;
  title = prefix + title;
  await tp.file.rename(title);
}

const subject = await tp.system.suggester(["高数", "线代", "概率论", "待分类"], ["高数", "线代", "概率论", "待分类"], false, "选择学科");
const chapter = await tp.system.prompt("章节标签（不带 #）") || "待分章";
const problemType = await tp.system.prompt("题型标签（不带 #，如 极限计算/矩阵相似）") || "待题型";
const status = await tp.system.suggester(["完全不会", "比较掌握", "完全掌握"], ["完全不会", "比较掌握", "完全掌握"], false, "掌握状态");
const rating = await tp.system.suggester(["5", "4", "3", "2", "1", "待评估"], ["5", "4", "3", "2", "1", "待评估"], false, "复习优先级");
const source = await tp.system.prompt("来源（书名/PDF页码/试卷），可留空") || "";
-%>
type: 错题
title: "[[<% tp.file.title %>]]"
tags:
  - <% subject %>
  - <% chapter %>
  - <% problemType %>
aliases:
  - <% tp.file.title %>
source: "<% source %>"
status: <% status %>
rating: <% rating %>
method:
cover: "[[<% tp.file.title %>.png]]"
date_creation: <% tp.file.creation_date("YYYY-MM-DD") %>
---

# <% tp.file.title %>

> [!example] 题图 / 题干
> ![[<% tp.file.title %>.png|650]]
>
> 题干文字：

## 正确入口

- 题目识别信号：
- 应调用的方法：[[相关方法]]
- 关键知识点：[[相关基础知识]]

## 解法

1.
2.
3.

## 错因复盘

> [!warning] 错因
> - 概念不清：
> - 计算失误：
> - 方法误选：
> - 条件漏看：
> - 其他：

## 关键一步

> [!tip] 下次先做这一步
>

## 二刷记录

- [ ] 二刷日期：
- [ ] 二刷结果：待确认
- [ ] 是否需要转成方法卡：

## 举一反三

- 同类信号：
- 可迁移方法：
- 关联题目：[[同类题目汇总]]

## 待确认

- [ ] 题图是否清晰：
- [ ] 公式 / 选项 / 页码是否准确：
